# SemanticSearch

Cette livraison prépare la future recherche sémantique.

La recherche est actuellement lexicale. Une future version remplacera ce moteur
par une recherche vectorielle (embeddings) sans modifier les appels de l'application.
